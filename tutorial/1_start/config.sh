# Git config - set user's name and email
## Notice: You need to replace <NAME> and <EMAIL> as your own
git config --global user.name "<NAME>"
git config --global user.email "<EMAIL>"

# Git config - list your git config
git config -l